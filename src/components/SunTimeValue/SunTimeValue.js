import styled from 'styled-components';
import SunTimeWrapper from './SunTimeWrapper';

const SunTimeValue = styled(SunTimeWrapper)`
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 50px;
`;

export default SunTimeValue;
